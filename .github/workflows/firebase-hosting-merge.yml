# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - dev
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: create env file
        run: |
          touch .env
          echo SKIP_PREFLIGHT_CHECK="${{ secrets.SKIP_PREFLIGHT_CHECK }}" >> .env
          echo REACT_APP_API_BASE_URL_DEV="${{ secrets.REACT_APP_API_BASE_URL_DEV }}" >> .env
          echo REACT_APP_API_IS_DEV="${{ secrets.REACT_APP_API_IS_DEV }}" >> .env
          cat .env
      - run: npm i --legacy-peer-deps
      - run: npm run build
        env:
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ERP_CLIENT_A065F }}'
          channelId: live
          projectId: erp-client-a065f
